# ──────────────────────────────────────────────────────────────────────
# IGEL Test Automation - Main Configuration
#
# SECURITY NOTE: Do NOT commit real passwords or secrets here.
# Store credentials in environment variables or a local .env file
# (which should be listed in .gitignore).
#
# Required environment variables:
#   UMS_PASSWORD          – UMS login password
#   OTP_SECRET_KEY        – Entra ID OTP secret
# ──────────────────────────────────────────────────────────────────────

device:
  host: "192.168.11.135"
  port: "22"
  user: "root"
  pwd: ""
  version : "12.8.0-1.rc.20"
  hostname: "ITC005056ADE39C"
  chrome: "chromium-143.0.7499.+1"

UMS:
  base_url: "https://192.168.10.28:8443/umsapi/v3"
  username: "laxmikanth.ghali.ums"
  password: "${UMS_PASSWORD}"
  weburl: "https://192.168.10.28:8443"
  current_device_version: "12.8.0-1.rc.20"
  previous_device_version: "12.8.0-1.rc.20"
  current_version_dir: "LG igel 12.8.0. rc20"
  previous_version_dir: "LG Igel 12.8.0 rc20"

logger:
# INFO DEBUG ERROR WARN, default INFO
  logLevel: "INFO"

otp_secret_cred:
  Entra_ID:
    testuser1:
      secret_key: "${OTP_SECRET_KEY}"
